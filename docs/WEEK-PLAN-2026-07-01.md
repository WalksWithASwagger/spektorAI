# Week Plan - 2026-07-01

Last refreshed: 2026-07-01

Tactical plan for the rest of this week (Wed 2026-07-01 through Fri 2026-07-03).
Living direction stays in [`../ROADMAP.md`](../ROADMAP.md),
[`NEXT-ROUND-PLAN-2026-05-19.md`](NEXT-ROUND-PLAN-2026-05-19.md), and
[`../STATUS.md`](../STATUS.md); this file is the short-horizon sequencing only.

## Starting State

- PR [`#59`](https://github.com/WalksWithASwagger/spektorAI/pull/59) (docs and
  structure audit) is open and green: `docs-check` and `python` pass, `review`
  skipped. Awaiting owner merge.
- No other open GitHub issues or PRs.
- Rule of the house: the next roadmap slice comes from fresh dogfood evidence,
  not from reopening the closed `#49`-`#52` queue.

## Wed 2026-07-01 - Land The Cleanup

- Merge PR `#59` after owner approval (merges need explicit human approval).
- After merge, confirm `main` is green and restart the working branch from the
  updated `main`.
- Verify the retired docs left no dangling references (`make docs-check`).

## Thu 2026-07-02 - Fresh Dogfood Evidence

- Run one real owner capture through the full loop: capture/import -> recipe run
  -> review -> markdown/Notion export -> run reopen -> resurfacing digest.
- Write a short friction note under `docs/dogfood/` with concrete blockers and
  missing controls, in the same shape as `docs/dogfood/2026-05-20-wispr-flow-loop.md`.
- Keep routing dry-run; do not flip external sends for the dogfood run.

## Fri 2026-07-03 - Scope One Next Lane

- From the dogfood friction plus the "Roadmap From Here" section of `ROADMAP.md`,
  pick exactly one lane and cut a scoped, `agent:ready`-shaped issue. Candidate
  lanes already teed up:
  - Transcription router: runtime validation and transcript assembly before any
    default change.
  - KB governance: real reviewer cleanup of stale or private-looking profile
    files.
  - Human-gated routing: digest publish/send and Notion task-page creation
    behind explicit approval.
  - SongForge: iterate from real creative-session feedback.
- Do not change runtime defaults without fixtures and a clear privacy/cost
  statement.

## Standing Guardrails

- Routing stays dry-run by default; merges, deploys, paid-provider swaps, and
  external sends need explicit human approval.
- Before opening a lane, run `make docs-check && make lint && make test && make smoke`.
- Keep diffs surgical and match existing style.
